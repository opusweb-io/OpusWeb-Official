// This component has been replaced by DynamicSprinkleBackground.tsx
// and is no longer used.
// Keeping the file to avoid breaking imports if any were missed,
// but its content is cleared.
export default function HeroBackground() {
  return null;
}
